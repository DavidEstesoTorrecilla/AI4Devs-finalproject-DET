{
  "name": "backend-gateway",
  "version": "1.0.0",
  "description": "EMASAI (Organis.AI) - API Gateway con FastAPI",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest tests/ -v",
    "lint": "ruff check .",
    "format": "black ."
  }
}
